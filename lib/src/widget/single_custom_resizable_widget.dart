import 'package:flutter/widgets.dart';

class SingleCustomResizableWidget extends StatelessWidget {
  const SingleCustomResizableWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}