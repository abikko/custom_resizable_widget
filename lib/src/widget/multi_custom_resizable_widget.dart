import 'package:flutter/widgets.dart';

class MultiCustomResizableWidget extends StatelessWidget {
  const MultiCustomResizableWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}